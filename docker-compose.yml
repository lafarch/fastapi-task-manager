version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./tasks.db:/app/tasks.db
    environment:
      - PYTHONUNBUFFERED=1
    command: fastapi dev app/main.py --host 0.0.0.0 --port 8000